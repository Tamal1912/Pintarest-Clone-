<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
    rel="stylesheet"
/>
  <link rel="stylesheet" href="stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

  <% if(nav === true){ %>
    <div class="w-full px-10 py-3 flex justify-between bg-zinc-300">
      <h3 class="font-mono text-lg">Pinterest</h3>
      <div class="flex gap-10 items-center font-sans font-semibold">
        <a class="hover:text-red-600 " href="/profile">Profile</a>
        <a href="#" class="hover:text-red-600 ">Feed</a>
        <a href="/logout" class="px-3 py-1 bg-red-600 text-white rounded-lg">Logout</a>
      </div>
    </div>
  <% } %>
